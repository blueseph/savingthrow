(function () {
    'use strict';

    angular.module("saving-throw")
            .controller("loginCtrl",
              ["$scope",
               "$location",
                loginCtrl]);

    function loginCtrl($scope, $location) {
    }
})();
