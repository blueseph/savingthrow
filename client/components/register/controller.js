(function () {
    'use strict';

    angular.module("saving-throw")
            .controller("registerCtrl",
              ["$scope",
               "$location",
                registerCtrl]);

    function registerCtrl($scope, $location) {
    }
})();
